---
# AWX installs these before every job run.
# The patchmon collection is also bundled under collections/ in this repo
# so it works even without Galaxy access.
collections:
  - name: ansible.posix
    version: ">=1.5"
  - name: community.general
    version: ">=7.0"
  - name: ansible.windows
    version: ">=2.0"
